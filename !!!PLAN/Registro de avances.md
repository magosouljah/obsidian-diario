# Registro de avances — BeatGaler

> Historial cronológico. Antes de trabajar: leer completo [`Plan Maestro.md`](./Plan%20Maestro.md). Cada avance relevante debe quedar aquí además de actualizar el estado vivo y la tarea de su fase.

- **2026-08-22 — Sistema de seguimiento del Plan Maestro.** Se añade `Estado vivo del plan`, la convención visual (`[ ]`, `[ 🟡 ]`, `[ ⚠️ ]`, `[ 🔴 ]`, `[ ⏸️ ]`, `[x]`) y este registro cronológico. No cambia el alcance técnico.
- **2026-08-22 — Tarea 0.1 completada: congelar evidencia.** Se creó `docs/RELEASE-LEDGER.md` en `magosouljah/BeatGaler`, rama `galer-cloud-v0.7.4`, con SHAs auditados Cloud `131df88753c812c0fdf440a5558fff46b2a83f57` y Web `e79728642839493326df706aba993a4cde2bdc02`, conteos de builds/pruebas, warnings, vulnerabilidades y límites no verificados. Estado `NO-GO`; sin tag público. Evidencia BeatGaler `712331a60ef5e04ca4c57fcb5c52ad13e2ea279b`.
- **2026-08-22 — Tarea 0.2 completada: checkpoint interno y backlog P0/P1.** El 4 de septiembre de 2026 queda como checkpoint interno sin cobros ni usuarios reales de producción. RO tiene autoridad final de stop-release y `0 P0/P1` sigue obligatorio. Backlog maestro: BeatGaler Issue #3, con owners por rol y evidencia de salida.
- **2026-08-22 — Tarea 1.1 completada: decisiones de negocio.** BeatGaler v1 será comercial/pagada y nunca tendrá fallback free-only; si billing falla, v1 se retrasa. Official Beta/promociones usan entitlements reales regalados. Mercados iniciales: México, EE. UU., Canadá, UE y Reino Unido; edad 18+; monedas MXN/USD/CAD/EUR/GBP; refund comercial base 14 días sujeto a derechos superiores; operación inicial desde México bajo estructura fiscal válida a confirmar; distribución directa Web + Windows NSIS + macOS DMG.
- **2026-08-22 — Tarea 1.2 iniciada: dependencias con lead time.** Sin dominio ni Apple Developer todavía; GitHub Releases previsto; support/status dependen del dominio. Objetivo macOS Intel + Apple Silicon; testers disponibles. Pendientes Authenticode, revisión legal, revisión independiente de seguridad y matriz física. Permanece `[ 🟡 ]`.
- **2026-08-22 — Tarea 2.1 completada con evidencia.** En Cloud se añadió contención HTTP: auth antes de Multer, ownership, límites de tamaño/rate/concurrencia, retirada de `/library/upsert` antes de multipart y registro público cerrado por defecto en producción. Límite técnico efectivo **1.99 GB = 1,990,000,000 bytes**. `npm run test:containment` → **PASS regression-http-containment**.
- **2026-08-22 — Tarea 2.2 en progreso: saneamiento de estado operacional rastreado.** Se confirmó información operacional concreta y se retiraron del HEAD `transport-pool-state.backup.json`, `transport-bots.json` y `transport-bots.local.json`. Configuración real queda privada/ignorada; `transport-bots.example.json` sanitizado. Historia Git aún requiere reauditoría y decisión de purga. No se rotaron/revocaron tokens sin evidencia de token en claro; revocación operativa sigue siendo requisito antes de escalar hacia ~80 bots o ante compromiso confirmado. Cloud hasta `626efe933cb61130d5f7d20bcdd398f53b61d434`.
- **2026-08-23 — Tarea 3.1 completada: base integrada Web/Desktop.** Se creó `integration-v0.8.0-alpha.1` desde Cloud `626efe933cb61130d5f7d20bcdd398f53b61d434`, se portó Web por capacidades, se reconciliaron conscientemente `App.tsx`, `Drawer.tsx`, `libraryStateManager.ts` y tests compartidos, se conservaron fixes Desktop/macOS, se fijó versión `0.8.0-alpha.1` y se limpió el árbol público. HEAD validado previo `4662a109bcc769774e33fe53182088c605846002`; CI #63 PASS Windows + macOS arm64 + x86_64; rama protegida.
- **2026-08-23 — Tarea 3.2 completada: contrato de plataforma.** Se corrigió un hueco del runner: `platformCapabilities.test.ts` existía pero no se ejecutaba. `src/platform/capabilities.ts` queda como matriz compartida e incluye `youtubePublishing`; Desktop queda protegido para Direct, Offline y YouTube. Se añadió guard DOM que falla si flujos Web invocan Tauri. Web mantiene Direct/Offline/YouTube en `false` como estado actual, no como exclusión permanente. PR BeatGaler #8, commit de trabajo `818214889ef3c6f97a262a91046f7df0e4f723fe`; CI PR #64 PASS Windows + macOS arm64 + x86_64; merge a `integration-v0.8.0-alpha.1` en `32a38c490a53650a0e9d6435c50cd009ef1b5123`; CI post-merge #65 PASS Windows + macOS arm64 + x86_64 antes del cierre. El ruleset quedó con `Required approvals = 0` porque existe un solo maintainer; se conservan PR + CI/checks y esto no elimina reviewers independientes exigidos por gates posteriores.
- **2026-08-23 — Plan Maestro dividido por unidades de decisión.** El monolito se divide en `Plan Maestro.md` como entrada obligatoria, contexto global, Fases 0–7, gates/contingencias y registro. El protocolo vigente queda: principal completo → fase activa completa → gates cuando aplique → auditoría read-only → cambio mínimo → tests/CI → actualización triple (principal + fase + registro). La reorganización no rebaja ni elimina gates.
- **2026-08-23 — YouTube Web incorporado directamente a Fase 2.** Tras revisar el plan completo, YouTube Web se mantiene en **Fase 2 — Flujos Web completos y rediseño de alto impacto**, porque es una capacidad funcional Web y no una tarea de producción/billing. La **Tarea 15.3 [P1 · FE/BE/QA] — Implementar YouTube Web sin Tauri** contiene ahora íntegramente contrato compartido, Desktop adapter, backend Web/OAuth, Web adapter, job/upload, UI y tests. Se elimina `Modulos/YouTube Web.md` y la carpeta `Modulos/`. Fase 3 conserva los gates posteriores que le corresponden: entorno productivo y quotas/entitlements; Fase 4 conserva la validación cross-platform. `youtubePublishing` Web solo pasa a `true` tras el gate funcional de 15.3 y v1 sigue bloqueada hasta superar también los gates posteriores de producción/release.
- **2026-08-23 — Tarea 4.1 completada: pipeline obligatorio.** PR BeatGaler #9 convirtió `Test - Desktop Portability` en un gate reproducible con Web build real + Chrome headless smoke, frontend/shared/backend/regresiones/packaging, Rust nativo en Windows + macOS arm64 + x86_64, Node `22.23.2`, Rust `1.98.0`, Actions fijadas por SHA, `npm ci`, Cargo `--locked`, `version:check` y un agregador estable `Required CI`. CI PR #68 terminó PASS 5/5; merge `c7894ad3c2b3e296e3d2939d73953b159e48852f`; CI post-merge #70 terminó PASS 5/5 incluido `Required CI`. El ruleset quedó confirmado con PR obligatorio, `Required CI` como único status check requerido, rama actualizada antes de merge y `Required approvals = 0` por existir un solo maintainer. Gate 4.1 satisfecho; siguiente tarea principal: 4.2.
- **2026-08-24 — Tarea 4.2 completada: supply chain conocida cerrada.** PR BeatGaler #10 actualizó Vitest/Vite/WebdriverIO/transitivas y añadió un gate de supply chain con `npm audit`, RustSec `cargo audit`, verificación de licencias npm/Cargo, Gitleaks HEAD-only fijado por versión+checksum, SBOM CycloneDX npm+Rust, checksums de lockfiles y registro/verificación de procedencia y digest para Node/FFmpeg/Bot API; además, Actions de CI/build/release quedan fijadas por SHA. Head final `902e4edf6f6f5d28f0f98922d5f22cc623c92f3d`; CI PR #100 (`32702389575`) PASS 6/6, incluido `Supply chain gate` y `Required CI`. El artefacto `supply-chain-evidence` id `9511091432` quedó con digest `sha256:d3b38c3be14ec01f0c283522049732a4e300588d8f0a9c588ec30221e0222419`. PR #10 mergeado como `f6d1f998bd63589ec2ddad7ee4d5818e9b85f016`; el merge sintético aprobado por CI y el merge real comparten árbol exacto `9a7000f3f3a1840ebae0310ac3df6b827561f2c5`, por lo que el contenido integrado es exactamente el probado. No se usó excepción temporal. Gate 4.2 satisfecho; siguiente tarea principal: 5.1.
- **2026-08-24 — Tarea 5.1 iniciada: límites de confianza Direct.** Auditoría read-only confirmó credenciales de infraestructura en clientes, Web descifrando el credential envelope, discovery confiando en `127.0.0.1:4000`, parser ID3 remoto y hardening Web/Tauri pendiente. Tras revisar primitivas oficiales de Telegram, PR BeatGaler #11 (`task-5.1-trust-boundaries`, head `5cdcfcecccea63a31adc5eaf66416929c0fbb95a`) añade solo evidencia de diseño: `ADR-0051-TRUST-BOUNDARIES`, `THREAT-MODEL-0051` y `MIGRATION-0051-ROLLBACK`. Se fija como requisito inmutable que los archivos viajan **dispositivo ↔ Telegram directamente**, nunca por Galer Cloud como relay. Candidata: MTProto temporary auth keys de bot renovables y RAM-only + membership limitada por vault + permisos mínimos; pin puede ser baseline si es imprescindible y privilegios delicados se elevan solo cuando Telegram lo exija mediante leases cortas con watchdog. Telegram documenta además que bots pueden borrar sus propios mensajes salientes en grupos/supergrupos, por lo que `can_delete_messages` no debe activarse para delete normal si el prototipo MTProto confirma el mismo comportamiento. CI PR #103 (`32755046082`) estaba en progreso al registrar este avance. **5.1 permanece en progreso y NO-GO:** faltan prototipo, renovación, prueba 1.9 GB, aislamiento cross-vault, medición de escalabilidad/flood limits, hardening restante y revisión independiente de seguridad.
- **2026-08-24 — Tarea 5.1: permission churn retirado del diseño principal y M0 acotado.** Se verificó que CI #103 de PR #11 terminó PASS 6/6 y que PR #12 (`task-5.1-permission-churn-probe`, head `2b8904880dfeaa57b970674a79abcb181161af0a`) también pasó CI #105 6/6. El probe #12 produjo 80 cambios administrativos exitosos en una primera corrida; una segunda corrida poco después, a 250 ms, recibió `FLOOD_WAIT 533s` después de 20 cambios de esa corrida y la restauración inmediata también recibió `FLOOD`. Como la actividad previa puede haber contribuido, no se afirma un límite de 20; sí queda probado que churn frecuente puede bloquear el cambio y su restore. Decisión: eliminar de la arquitectura principal `grant -> operación -> revoke`, promote/demote por operación y privilege leases dependientes de restore; `pin_messages` y, si delete cross-bot lo requiere, `delete_messages` se evaluarán como baseline mínimo estable. PR #11 fue actualizado solo en documentación hasta `bb162c01c80e21a264c4022c9c682a90c14fbb98`; sigue `DRAFT` y requiere CI nuevo. La especificación oficial permite plantear M0 como split: permanent auth en lado controlado construye el binding cifrado y el dispositivo conserva la temp key y envía `auth.bindTempAuthKey` directamente a Telegram. Esto sigue siendo hipótesis hasta prueba real. **5.1 permanece `[ 🟡 ]` / NO-GO; ningún checkbox se marca `[x]`.**
- **2026-08-24 — Tarea 5.1 M0-A: frontera criptográfica sintética implementada sin tocar runtime.** Se creó desde el baseline exacto `f6d1f998bd63589ec2ddad7ee4d5818e9b85f016` la rama `task-5.1-temp-auth-binding-probe` y el draft PR BeatGaler #13 `test(security): prove M0 temp-auth binding boundary`, head `96af35e85481ff85d856dc22949bfb314ebedc3e`. El diff confirmado son solo `scripts/telegram-temp-auth-binding-vector.mjs` y una línea en `package.json` para ejecutar el vector dentro de `test:regressions`. El probe no usa red ni credenciales/vault reales; valida AES-IGE con un vector publicado, serializa `bind_auth_key_inner`, deriva `msg_key`/AES MTProto v1 y verifica round-trip con material sintético. Su interfaz de binder no acepta bytes de la temporary auth key, por lo que demuestra que esa pieza local puede construirse manteniendo permanent auth del lado binder y la temp key fuera de él; también registra explícitamente `network_bind_proven=false` y `direct_mtproto_operation_proven=false`. Self-test aislado: PASS. CI #109 está en progreso. CI #108 del head documental corregido de #11 también sigue en progreso al último chequeo; ya habían pasado Web/shared, Supply chain, Windows y macOS arm64. **No se tocó Direct productivo, Bot API, Offline, YouTube, Pinterest, UI, token rotation/revoke, vaults ni datos. 5.1 continúa `[ 🟡 ]` / NO-GO.**
- **2026-08-24 — Tarea 5.1: mtcute hace viable reutilizar el handshake, pero su PFS integrado no satisface el límite de confianza.** CI #108 del head documental corregido de PR #11 terminó PASS 6/6 incluido `Required CI`. En PR #13, CI #109 ya pasó Web/shared, Supply chain, Windows y macOS arm64; macOS x86_64 sigue ejecutándose, así que no se registra todavía PASS final. Los logs Web/shared ejecutaron el nuevo M0-A y conservaron explícitamente `network_bind_proven=false` y `direct_mtproto_operation_proven=false`. Auditoría read-only de mtcute confirmó que `doAuthorization(..., expiresIn)` ya implementa creación de temporary auth keys/PFS y que `SessionConnection` tiene TTL/refresh, pero exige una permanent auth key en el mismo cliente antes del bind; por tanto activar `usePfs` tal cual violaría 5.1. BeatGaler Web ya usa `MemoryStorage`, compatible con RAM-only. `MtClientOptions` exige `apiHash`, pero el constructor no pasa `apiHash` a `NetworkManager`, solo `apiId`, por lo que M0-B puede investigar un adapter de red más bajo sin API hash ni permanent auth en cliente. **Siguiente gate, todavía aislado:** cliente genera/conserva temp key, binder controlado construye el binding con permanent auth, cliente envía `auth.bindTempAuthKey` y una operación RPC directamente a Telegram. 5.1 continúa `[ 🟡 ]` / NO-GO.
- **2026-08-24 — Tarea 5.1 M0-A: CI requerido completado.** PR #13, head `96af35e85481ff85d856dc22949bfb314ebedc3e`, terminó CI #109 PASS 6/6 incluido `Required CI`: Web/shared, Supply chain, Windows, macOS arm64 y macOS x86_64 success. Esto valida portabilidad/regresiones del probe sintético, no el bind real: `network_bind_proven=false` y `direct_mtproto_operation_proven=false` siguen siendo la evidencia correcta. **5.1 permanece `[ 🟡 ]` / NO-GO y M0-B sigue siendo el siguiente gate.**
- **2026-08-24 — Tarea 5.1 M0-B0: seam real de mtcute verificado en CI sin tocar runtime.** Se abrió el draft PR #14 `test(security): audit mtcute seam for M0-B`, rama `task-5.1-temp-auth-network-probe`, apilado sobre PR #13 para mantener el incremento aislado. Head final probado `9bd8bee1eda87cbeab051a3937ef95f6c4884ec4`; diff = `scripts/telegram-temp-auth-mtcute-seam.mjs` + una línea en `package.json`. El probe no usa red, secretos, bot ni vault y examina la dependencia instalada, no una copia asumida del source. CI #110 detectó una aserción demasiado literal sobre comillas del CJS publicado; se corrigió a verificación semántica sin cambiar producto. CI #111 terminó PASS 6/6 incluido `Required CI`: Web/shared, Supply chain, Windows, macOS arm64 y macOS x86_64 success. Output ejecutado: `@mtcute/web 0.31.0` + `@mtcute/core 0.31.0`, generación de temp auth key presente, bind enviado bajo temp key presente y rolling refresh presente; a la vez `permanent_key_bytes_used_by_stock_pfs=true` y `stock_use_pfs_satisfies_task_5_1=false`. El probe conserva `network_bind_proven=false`, `direct_mtproto_operation_proven=false` y `galer_file_bytes=false`. **Siguiente gate M0-B1:** bind real permanent-side/temp-side directamente contra Telegram, preferentemente test DC, todavía sin tocar Direct productivo. 5.1 continúa `[ 🟡 ]` / NO-GO.
- **2026-08-24 — Tarea 5.1 M0-B1: split network bind aceptado por Telegram TEST DC.** Draft PR #15 `test(security): prove M0-B1 split temp-auth bind`, rama `task-5.1-temp-auth-live-bind-probe`, head limpio `2b942deea108fc4818bbb1c088db2f144f3c42c0`, apilado sobre #14. El harness mantiene permanent auth en un proceso binder separado y temp auth en el cliente; no usa bot token, API hash, usuario BeatGaler, vault ni archivos. El cliente comparte únicamente metadata/IDs de binding; el binder devuelve permanent key id + `encrypted_message`; y el cliente envía `auth.bindTempAuthKey` directamente a Telegram cifrado bajo la temp key. Telegram devolvió `boolTrue`: `network_bind_proven=true`, `permanent_auth_reaches_client=false`, `temp_auth_key_reaches_binder=false`, `galer_file_bytes=false`. Probe run #16 PASS y CI del mismo head #117 PASS 6/6 incluido `Required CI`. Un experimento posterior de RPC post-bind devolvió `mt_rpc_error`; se retiró del scope probado y el head fue restaurado al bind exitoso, por lo que `bot_identity_proven=false` y `direct_mtproto_operation_proven=false` siguen siendo correctos. **Siguiente gate M0-B2:** identidad bot autorizada + operación MTProto directa manteniendo permanent bot auth/credenciales solo del lado controlado, sin vault real ni archivo de usuario. 5.1 continúa `[ 🟡 ]` / NO-GO; no se marca ningún `[x]`.
- **2026-08-24 — Tarea 5.1 M0-B2: harness aislado listo, live pendiente por secretos dedicados.** Se abrió el draft PR #16 `test(security): prepare M0-B2 bot temp-auth proof`, rama `task-5.1-temp-auth-bot-probe`, head `43c43353dce5a025340bd142222a57583ba4a609`, apilado sobre el B1 limpio. El diff son solo `scripts/telegram-temp-auth-bot-live.mjs` y `.github/workflows/probe-task-5.1-temp-auth-bot.yml`. El parent/binder es el único proceso que puede recibir `BEATGALER_M0_B2_API_ID`, `BEATGALER_M0_B2_API_HASH` y `BEATGALER_M0_B2_BOT_TOKEN`; autoriza allí la permanent auth key como bot, mientras el cliente se lanza con esos secretos eliminados de su environment, genera/conserva la temporary auth key, hace el bind directo y solo aprobaría B2 si `users.getUsers(inputUserSelf)` devuelve exactamente la misma identidad bot. Workflow B2 #1 pasó sintaxis/harness pero los tres secretos dedicados estaban ausentes, por lo que el live step quedó SKIPPED y registró explícitamente `B2_LIVE_PROOF=NOT_RUN`. CI normal #128 terminó PASS 6/6 incluido `Required CI`. Esto valida el harness y que no rompe portabilidad, **no** la identidad bot: `bot_identity_proven=false` y `direct_mtproto_operation_proven=false` siguen vigentes. No se tocó runtime productivo, vaults, archivos, Offline, YouTube, Pinterest, UI ni token rotation/revoke. **Siguiente paso:** configurar esos tres secretos únicamente en GitHub Actions, sin compartir valores en chat/código/logs, y reejecutar el probe B2. 5.1 continúa `[ 🟡 ]` / NO-GO; ningún `[x]`.
- **2026-08-24 — Tarea 5.1 M0-B2: identidad bot + RPC directo probados con split de credenciales.** Tras configurar los tres secrets B2 solo en GitHub Actions, `auth.importBotAuthorization` devolvió inicialmente `USER_MIGRATE_1`; se corrigió únicamente el harness para seguir el home DC en el binder/control side y comunicar al cliente solo el DC como metadata. En el head final `5ff0d70edd6c4ac11ff54bbd52a68246342130ac` del draft PR #16 `test(security): prove M0-B2 bot temp-auth identity`, el workflow `32789070730`, rerun job `97626995224`, terminó PASS limpio en production DC 1: `bot_identity_proven=true`, `network_bind_proven=true`, `direct_mtproto_operation_proven=true`, `permanent_auth_reaches_client=false`, `bot_token_reaches_client=false`, `api_hash_reaches_client=false`, `galer_file_bytes=false`, `vault_used=false` y `token_rotation_or_revoke=false`. Una primera ejecución de ese mismo head falló antes del bind con `Step 4: invalid nonce hash from server`; el rerun sin cambios pasó y se clasifica como ruido transitorio del handshake. Un head anterior ya había emitido el mismo PASS pero quedó vivo hasta el timeout por cleanup; el head final acota ese cleanup y termina verde. CI normal #130 (`32789070736`) seguía en curso al registrar este avance: Web/shared, Supply chain, Windows y macOS arm64 ya PASS; macOS x86_64/`Required CI` pendientes. **Siguiente gate M0-C:** renovación proactiva, expiración anticipada controlada y continuidad de operación lógica larga atravesando renovación, sin permanent credentials en cliente y sin vault/archivo real. 5.1 continúa `[ 🟡 ]` / NO-GO; ningún `[x]`.
- **2026-08-24 — Tarea 5.1 M0-B2: CI requerido del head probado completado.** CI normal #130 (`32789070736`) del mismo head `5ff0d70edd6c4ac11ff54bbd52a68246342130ac` terminó PASS 6/6: Web/shared, Supply chain, Windows, macOS arm64, macOS x86_64 y `Required CI`. Junto con el workflow B2 limpio `32789070730` / job `97626995224`, queda cerrada la evidencia de M0-B2 como incremento experimental: identidad bot heredada + RPC directo sí probados sin permanent auth/token/API hash en el cliente; archivo/vault/renovación todavía no. **Siguiente gate: M0-C. 5.1 continúa `[ 🟡 ]` / NO-GO; ningún `[x]`.**